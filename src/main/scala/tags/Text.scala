package tags

import org.scalajs.dom.Node
import org.scalajs.dom.document

// final case class Text(text: String) extends Tag:
//   def contents = Seq.empty
//   def attrs = Seq.empty
//   val name: String = "text"
//   override def render: Node = document.createTextNode(text)
